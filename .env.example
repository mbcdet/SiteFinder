# Copy to .env and fill in as needed. All optional for OSM-only runs.
# SITEFINDER_DB_PATH=sitefinder.db
# SITEFINDER_FRESHNESS_DAYS=30
# Google Places enrichment (only needed when running `--enrich places`):
# SITEFINDER_GOOGLE_PLACES_API_KEY=your_key_here
